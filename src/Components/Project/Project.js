import React from 'react';
import "./Project.css"

const Project = () => {
      return (
            <div className='project'>
                  <h1>Nothing has been added here for now</h1>
            </div>
      );
};

export default Project;